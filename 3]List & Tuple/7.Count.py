#WAP to count the number of students with the "A" grade in the following tuple
#Also store the above values in a list & sort them from "A" to "D" aka ascending order

grade = ("C", "A", "D", "A", "B", "B", "A")
print(grade.count("A"))

grade = ["C", "A", "D", "A", "B", "B", "A"]
grade.sort()
print(grade)

